<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.0.3</version>
    <relativePath/>
  </parent>
  <groupId>fr.simplex-software.workshop</groupId>
  <artifactId>k8s-sb</artifactId>
  <version>1.0-SNAPSHOT</version>
  <properties>
    <jdk.version>21</jdk.version>
    <java.version>${jdk.version}</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Spring Boot 4 manages Groovy 5, but REST Assured 5.5.0 (test only) is
         built against Groovy 4 and its Groovy request pipeline breaks on Groovy 5.
         Pin Groovy to the 4.0.x line REST Assured expects. Groovy is a test-scope
         transitive of REST Assured only; the application itself uses no Groovy. -->
    <groovy.version>4.0.22</groovy.version>
  </properties>
  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <!-- Tier 1 (unit): MockMvc slice with @WithUserDetails - fast, no cluster. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Tier 2 (e2e, *IT via failsafe): REST Assured hits the deployed app over
         real mTLS using the cluster-issued certs; skipped when no cluster is up. -->
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
      <version>5.5.0</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>
      <!-- Runs the *IT e2e tests in the integration-test/verify phases (surefire
           runs the *Test unit tier). The e2e tests self-skip when no cluster is
           reachable, so `mvn verify` stays green on a bare checkout. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-failsafe-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>integration-test</goal>
              <goal>verify</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <image>
            <name>nicolasduminil/k8s-sb:buildpacks</name>
            <env>
              <BP_JVM_VERSION>${jdk.version}</BP_JVM_VERSION>
            </env>
            <publish>true</publish>
          </image>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>